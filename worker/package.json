{
  "name": "maxbridge-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker: license server, Stripe webhook, install.sh/.md templating for Maxbridge.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "wrangler": "^3.95.0"
  }
}
